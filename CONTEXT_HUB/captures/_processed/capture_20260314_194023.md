timestamp: 2026-03-14T19:40:23
source: claude-direct
tags: pipeline, rileyfile, infrastructure, capture-skill
---
Session summary: Pipeline consolidation & capture skill fixes (2026-03-14)

Key changes made this session:

1. PERMISSIONS STREAMLINED — Added broad allow rules for pipeline commands (ls, wc, cat, tail, head, grep, git, python3, bash) so skills don't stall on approval pop-ups. Critical for phone/remote sessions where pop-ups aren't visible.

2. TWO-REPO SPLIT FIXED — Discovered ~/dev/rileyfile (local) and iCloud RileyFile were diverged repos both pushing to the same remote. launchd was pointing at iCloud (broken due to evicted files causing "Resource deadlock avoided" errors). Fixed by:
   - Removing RILEYFILE_ROOT from launchd plist (now defaults to local repo)
   - Adding iCloud→local pull step to sync_context.sh (pulls new captures from iCloud inbox before each pipeline run)

3. CAPTURE SKILL PATH — Updated /capture skill to write to iCloud inbox (not local repo) so it works from any device (laptop, desktop, phone, iOS app). Pipeline pulls from iCloud automatically.

4. CFA SCHEDULING CAPTURE — Brain dump about Chick-fil-A scheduling rules was captured and ingested (openers, leader timing, busy day patterns, minor restrictions, position constraints).

Architecture now:
- Phone shortcut → iCloud inbox
- /capture (any device) → iCloud inbox
- sync_context.sh pulls iCloud → local inbox → pipeline processes → git push
- Single source of truth: ~/dev/rileyfile
